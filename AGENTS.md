# AGENTS.md

This repository follows a single set of agent instructions used by all AI coding agents (GitHub Copilot, Claude, Cursor, Aider, Continue, etc.).

**👉 Read [.github/copilot-instructions.md](.github/copilot-instructions.md) before making changes.**

Key rule: **`README.md` must always reflect the current state of the repo.** Any change that adds/removes/renames a file, changes a folder, swaps a dependency, alters a feature, or affects the privacy posture **must update `README.md` in the same change**. See the linked file for the full list of triggers and conventions.
